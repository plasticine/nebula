[Unit]
Description=Docker GCR auth

[Timer]
# Time to wait after booting before we run first time
OnBootSec=1min
# Time between running each consecutive time
OnUnitActiveSec=10min
Unit=docker-gcr-auth.service

[Install]
WantedBy=multi-user.target
