{
  "variables": {
    "base_image_name": ""
  },
  "builders": [
    {
      "name": "nat",
      "type": "googlecompute",
      "zone": "us-central1-a",
      "source_image": "{{user `base_image_name`}}",
      "project_id": "{{user `googlecompute_project_id`}}",
      "account_file": "{{user `config_path`}}/account.json",
      "ssh_username": "ubuntu",
      "image_name": "nebula-ubuntu-nat-{{timestamp}}",
      "image_description": "NAT image."
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "scripts": [
        "{{template_dir}}/scripts/nat.bash"
      ]
    }
  ]
}
